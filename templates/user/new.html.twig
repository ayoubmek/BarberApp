
<h1>Create New User</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-2">Save</button>
{{ form_end(form) }}
<a href="{{ path('user_index') }}" class="btn btn-secondary mt-2">Back to list</a>